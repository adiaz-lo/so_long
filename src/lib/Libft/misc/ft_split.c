/*int   ft_count_words(char const *s, char c)
{
        int                     tnum;

        tnum = 0;
        if (!s || *s == '\0')
                return (0);
        while (*s != '\0')
        {
                if (*s == c && *(s - 1) != c)
                        tnum++;
                s++;
        }
        if (*(s - 1) != c)
                tnum++;
        return (tnum);
}*/
